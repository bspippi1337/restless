# Roadmap

## v2 (this repo)
- Domain-first discovery (one input)
- OpenAPI JSON + YAML parsing
- sitemap.xml docs discovery (light)
- Seed-only fuzzer expansion + safe verify
- Doctor service (clean + report)
- Help system: `--help` short, `help` long + TUI help tab

## v2.1
- Preset save/load wired in UI
- Request send + response viewer
- Export findings to JSON
- Rate-limit controls in UI

## v3
- optional deep crawl (opt-in)
- optional JS bundle hints (capped)
- native GUI wrapper (real window)
