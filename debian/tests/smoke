#!/bin/sh
set -eu

command -v restless

restless --version

restless --help >/dev/null

tmpdir="$(mktemp -d)"
trap 'rm -rf "$tmpdir"' EXIT

cd "$tmpdir"

touch test.txt

restless watch . --run 'echo ok' >/dev/null 2>&1 &
PID=$!

sleep 2

kill "$PID" || true
wait "$PID" 2>/dev/null || true
