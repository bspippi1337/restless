
## FAIL rc=1

Source: `archive/legacy/docs/CLI.md:10`

Command:

```bash
restless openapi ls
```

Output:

```text
Error: unknown command "openapi" for "restless"
Run 'restless --help' for usage.
unknown command "openapi" for "restless"

```

## FAIL rc=2

Source: `archive/legacy/docs/CLI.md:11`

Command:

```bash
restless openapi endpoints <id>
```

Output:

```text
bash: -c: line 1: syntax error near unexpected token `newline'
bash: -c: line 1: `/home/pippi/restless/build/restless openapi endpoints <id>'

```

## FAIL rc=1

Source: `archive/legacy/docs/CLI.md:12`

Command:

```bash
restless openapi run <id> GET /path
```

Output:

```text
bash: line 1: id: No such file or directory

```

## FAIL rc=1

Source: `archive/legacy/docs/CLI.md:17`

Command:

```bash
restless profile use dev
```

Output:

```text
Error: unknown command "profile" for "restless"
Run 'restless --help' for usage.
unknown command "profile" for "restless"

```

## FAIL rc=1

Source: `archive/legacy/docs/CLI.md:18`

Command:

```bash
restless profile ls
```

Output:

```text
Error: unknown command "profile" for "restless"
Run 'restless --help' for usage.
unknown command "profile" for "restless"

```

## FAIL rc=1

Source: `archive/legacy/docs/CLI.md:22`

Command:

```bash
restless prefs show
```

Output:

```text
Error: unknown command "prefs" for "restless"
Run 'restless --help' for usage.
unknown command "prefs" for "restless"

```

## FAIL rc=1

Source: `archive/legacy/docs/CLI.md:23`

Command:

```bash
restless prefs set color=on
```

Output:

```text
Error: unknown command "prefs" for "restless"
Run 'restless --help' for usage.
unknown command "prefs" for "restless"

```

## FAIL rc=1

Source: `archive/legacy/docs/DEMO.md:8`

Command:

```bash
restless list
```

Output:

```text
Error: unknown command "list" for "restless"
Run 'restless --help' for usage.
unknown command "list" for "restless"

```

## FAIL rc=1

Source: `archive/legacy/docs/DEMO.md:9`

Command:

```bash
restless run GET /headers
```

Output:

```text
Error: unknown command "run" for "restless"
Run 'restless --help' for usage.
unknown command "run" for "restless"

```

## FAIL rc=1

Source: `archive/legacy/docs/DEMO.md:10`

Command:

```bash
restless session
```

Output:

```text
Error: unknown command "session" for "restless"

Did you mean this?
	version

Run 'restless --help' for usage.
unknown command "session" for "restless"

Did you mean this?
	version


```

## FAIL rc=1

Source: `archive/legacy/docs/WHY_RESTLESS.md:77`

Command:

```bash
restless profile use prod
```

Output:

```text
Error: unknown command "profile" for "restless"
Run 'restless --help' for usage.
unknown command "profile" for "restless"

```

## FAIL rc=1

Source: `archive/legacy/docs/WHY_RESTLESS.md:98`

Command:
