# CLI reference (generated)

This file is generated from `./build/restless --help` and command help pages.

## restless --help

```text
API discovery and probing tool

Usage:
  restless [command]

Available Commands:
  auto        Autonomous recon: discover endpoints then launch swarm probing
  blckswan    Killer recon: auto-discover + OpenAPI + GraphQL + topology + README-ready exports
  completion  Generate the autocompletion script for the specified shell
  discover    discover API endpoints
  help        Help about any command
  inspect     
  magiswarm   API recon engine: discover, fuzz, map, report
  map         generate API topology
  octoswan    Swan brain + octopus: parallel probing + signal inference + README-ready artifacts
  scan        
  smart       Trigger remote smart probe via GitHub Actions
  swarm       Distributed API probing swarm

Flags:
  -h, --help   help for restless

Use "restless [command] --help" for more information about a command.
```

